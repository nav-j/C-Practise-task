
## **Section A: Short Answer Questions**

*(Answer in 1–2 lines)*

1. What is the purpose of `#include <stdio.h>` in a C program?
2. What does the `printf()` function do?
3. Write the correct syntax of the `main()` function in C.
4. What is the role of `return 0;` in the `main()` function?
5. State whether C language ignores white spaces or not.
6. What is the main purpose of the `main()` function in a C program?
7. Why is a semicolon (`;`) used at the end of C statements?
8. What is the output of `printf("Hello World!");`?
9. Name the function used to print output on the screen in C.
10. What does the `\n` escape character represent?
11. Can we use more than one `printf()` function in a program?
12. How are statements executed in a C program?

---

## **Section B: Long Answer Questions**

*(Answer in detail)*

1. **Explain the purpose of `#include <stdio.h>` and `printf()` in a C program.**
   Write a simple example to demonstrate their use.

2. **Explain the structure of a basic C program.**
   Describe the role of:

   * Header files
   * `main()` function
   * `return 0;` statement

3. **What is the `main()` function?**
   Explain why it is mandatory in every C program with a suitable example.

4. **Explain escape characters in C.**
   What is the use of `\n`? Write a program to print output on multiple lines.

5. **Write a C program to print the following output and explain each line of the code:**

   ```
   Hello World!
   I am learning C.
   ```

6. **Explain the importance of semicolons and white spaces in C programming.**
   Include examples to support your explanation.

7. **How can you create blank lines between printed output in C?**
   Explain using `printf()` with suitable code examples.
