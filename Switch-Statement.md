Here are **practice tasks using `switch` in C without user input** (values are already assigned in the program). These are great for exams, worksheets, and logic-building practice.

---

## 🔹 Task 1 — Fixed Day Program

Create a program where a variable `day = 5;` is already declared.
Use a `switch` statement to print the day name.

---

## 🔹 Task 2 — Fixed Calculator

Declare:

```c
int a = 10, b = 5;
char op = '*';
```

Use `switch` to perform the operation and print the result.

---

## 🔹 Task 3 — Fixed Character Check

Declare:

```c
char ch = 'E';
```

Use `switch` to check whether it is a vowel or consonant.

---

## 🔹 Task 4 — Grade Message

Declare:

```c
char grade = 'B';
```

Use `switch` to print grade message.

---

## 🔹 Task 5 — Menu Simulation

Declare:

```c
int choice = 2;
```

Menu:

```
1 → Hello
2 → Welcome
3 → Bye
```

Print message according to choice using `switch`.

---

## 🔹 Task 6 — Month Name

Declare:

```c
int month = 8;
```

Print month name using `switch`.

---

## 🔹 Task 7 — Even/Odd Checker (Using Switch Trick)

Declare:

```c
int num = 7;
```

Use:

```
switch(num % 2)
```

Print whether number is Even or Odd.

---

## 🔹 Task 8 — Simple Color Code

Declare:

```c
char color = 'R';
```

Switch cases:

* R → Red
* G → Green
* B → Blue

---
